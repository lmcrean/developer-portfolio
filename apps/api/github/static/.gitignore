# Static data files - generated by CI/CD pipeline
# These files should not be committed to the repository
# They are generated fresh during deployment

# Pull request data files
pull-requests/
*.json

# But keep directory structure for deployment
!.gitignore
!README.md